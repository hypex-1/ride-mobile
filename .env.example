# 📱 RideMobile Environment Configuration

## Backend Integration

```bash
# Development Environment
API_URL=http://localhost:3000/api
WS_URL=ws://localhost:3000
NODE_ENV=development

# API Endpoints
AUTH_ENDPOINT=/auth
USERS_ENDPOINT=/users
RIDES_ENDPOINT=/rides
PAYMENTS_ENDPOINT=/payments
DRIVERS_ENDPOINT=/drivers

# External Services (Add your keys)
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_key_here
GOOGLE_MAPS_API_KEY=your_google_maps_key_here

# App Configuration
APP_NAME=RideMobile
APP_VERSION=1.0.0
DEBUG_MODE=true

# Real-time Configuration
SOCKET_TIMEOUT=5000
RECONNECT_ATTEMPTS=5
RECONNECT_DELAY=1000

# Location Configuration
LOCATION_ACCURACY=6
LOCATION_UPDATE_INTERVAL=5000
MAP_INITIAL_ZOOM=15

# UI Configuration
THEME_MODE=auto
LANGUAGE=en
CURRENCY=USD
```

## Setup Instructions

1. Copy this file to `.env` in your project root
2. Replace placeholder values with your actual API keys
3. Update API_URL for production deployment
4. Configure external service keys (Stripe, Google Maps)

## Environment Variables

- **Required**: API_URL, WS_URL
- **Optional**: All external service keys (for full functionality)
- **Development**: All debug and development flags